import 'package:flutter/widgets.dart';

class LatesMoviesGrid extends StatelessWidget {
  const LatesMoviesGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
